function even_or_odd(number) {
    // given a number determine if even or odd
    
    // if number can be divided by 2, it is even
    
    // if number cannot be divided by 2, it is odd
    
    if (number % 2 === 0) {
      return "Even"
    } else {
      return "Odd"
    }
  }