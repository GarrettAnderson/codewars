// function positiveSum(arr) {
//   // map over each element in the arry
//   // determine which numbers in the array are negative
//   var posNum = arr.map((number) => (number > 0 ? number : 0))
//   console.log(posNum)
//   var sum = posNum.reduce((a, b) => a + b, 0)
//   console.log(sum)
//   return sum
// }
