function numberToString(num) {
    // Return a string of the number here!
    
    let numString = parseInt(num)
    console.log(numString)
    console.log(`${numString}`)
    return `${numString}`
  }